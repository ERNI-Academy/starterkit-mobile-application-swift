# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SwiftERNIStarterKit (iOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftERNIStarterKit (iOS)
  pod 'FlagKit'

end

target 'SwiftERNIStarterKit (macOS)' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftERNIStarterKit (macOS)
  pod 'FlagKit'

end
